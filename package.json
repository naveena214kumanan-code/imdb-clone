{
  "name": "imdb-clone",
  "version": "1.0.0",
  "private": true,
  "description": "IMDB Clone project with React frontend",
  "scripts": {
    "start": "echo 'Run the React app from ./frontend using npm start or deploy via Vercel'"
  }
}
